// api.js
// Centralised Axios/Fetch client for the InsightHub frontend.
// Configures base URL from REACT_APP_API_BASE_URL, attaches auth headers,
// and exports typed functions for every backend endpoint:
//   getMetrics(), getInsights(), postSearch(), getReportEmbedToken(), etc.
