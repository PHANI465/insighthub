// App.jsx
// Root React component for InsightHub.
// Sets up the React Router structure, global context providers
// (AuthContext, ThemeContext), and renders the NavSidebar alongside the
// active page component. All page-level routing is defined here.
