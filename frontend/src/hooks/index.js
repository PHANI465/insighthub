// hooks/index.js
// Barrel export for all custom React hooks in InsightHub.
// Will include: useMetrics (fetches KPIs), useInsights (polls AI insights),
// useSearch (debounced search calls), usePowerBIEmbed (embed token management),
// and useAuth (token refresh / session handling).
