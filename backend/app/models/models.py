# models.py
# SQLAlchemy ORM table definitions and Pydantic request/response schemas.
# ORM models mirror the Azure SQL schema (customers, products, sales, events).
# Pydantic schemas define the API contract — what the frontend sends and receives.
