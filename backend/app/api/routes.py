# routes.py
# Defines all FastAPI route handlers for InsightHub.
# Includes endpoints for:
#   GET  /api/insights      — AI-generated business insights
#   GET  /api/metrics       — KPI metrics and trend data
#   POST /api/search        — Natural-language search via Azure AI Search
#   GET  /api/reports       — Power BI report metadata and embed tokens
#   GET  /api/health        — Health check
